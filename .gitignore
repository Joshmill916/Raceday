# Private code generator — must NEVER be committed (holds the secret salt).
# Keep this file only on the owner's own device.
raceday-codegen.html

# How-to video recorder and output — local only, not part of the app
record-demo.js
add-audio.py
videos/

# Claude Code internal directory — settings and worktrees, not part of the app
.claude/
