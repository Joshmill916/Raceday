{
  "name": "raceday-functions",
  "private": true,
  "description": "Server-side backend for RaceDay/Driven — mints license and premium codes automatically after a Stripe payment. Never deployed to GitHub Pages; deployed only via `firebase deploy --only functions`.",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "test": "node lib/codegen.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.1.0",
    "stripe": "^17.3.0"
  }
}
